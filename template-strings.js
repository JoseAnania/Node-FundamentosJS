const name = 'janania';
const real = 'jose';

// Formas de concatenar
const realName = name + ' ' + real;
const template = `${name} ${real}`;

console.log(realName);
console.log(template);

console.log(realName === template);

const hello = `
Hola
Mundo
`;

console.log(hello);